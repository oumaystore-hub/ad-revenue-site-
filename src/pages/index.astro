<style>
  /* ✅ Mobile Optimizations - أضف هذا في نهاية الـ style */
  
  @media (max-width: 768px) {
    /* General */
    .container { padding: 0 1rem; }
    
    /* Hero Section */
    .hero {
      padding: 3rem 0 2.5rem;
      min-height: auto;
    }
    .hero .container {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    .hero-content {
      text-align: center;
      padding: 0 0.5rem;
    }
    .hero-badge {
      justify-content: center;
      margin-bottom: 1rem;
      font-size: 0.85rem;
      padding: 0.4rem 1rem;
    }
    .hero-title {
      font-size: 1.8rem;
      line-height: 1.3;
      margin-bottom: 1rem;
    }
    .hero-subtitle {
      font-size: 1rem;
      line-height: 1.6;
      margin-bottom: 1.5rem;
      padding: 0 0.5rem;
    }
    .hero-cta {
      flex-direction: column;
      align-items: stretch;
      gap: 0.8rem;
    }
    .btn-lg {
      padding: 1rem 1.5rem;
      font-size: 1rem;
      text-align: center;
      justify-content: center;
    }
    .hero-image {
      display: none; /* إخفاء البطاقات العائمة على الموبايل */    }
    
    /* Stats Section */
    .stats-section {
      margin-top: -2rem;
      padding: 2rem 0;
    }
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
      padding: 0 1rem;
    }
    .stat-card {
      padding: 1.5rem 1rem;
    }
    .stat-number {
      font-size: 2rem;
    }
    .stat-label {
      font-size: 0.9rem;
    }
    
    /* About Section */
    .about-section {
      padding: 3rem 0;
    }
    .about-grid {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }
    .section-title {
      font-size: 1.8rem;
      justify-content: center;
      text-align: center;
      margin-bottom: 1.5rem;
    }
    .title-icon {
      font-size: 2.2rem;
    }
    .about-content {
      padding: 0 0.5rem;
    }
    .about-text {
      font-size: 1rem;
      line-height: 1.8;
      margin-bottom: 1.2rem;
      text-align: justify;
    }
    .about-features {
      margin-top: 2rem;      gap: 1.2rem;
    }
    .feature-item {
      gap: 0.8rem;
      padding: 0.5rem;
    }
    .feature-icon {
      font-size: 1.6rem;
      flex-shrink: 0;
    }
    .feature-text h4 {
      font-size: 1rem;
      margin-bottom: 0.2rem;
    }
    .feature-text p {
      font-size: 0.9rem;
      line-height: 1.5;
    }
    
    /* About Image */
    .about-image {
      order: -1;
      padding: 0 1rem;
    }
    .image-wrapper {
      border-radius: 16px;
    }
    .main-img {
      border-radius: 16px;
    }
    .experience-badge {
      position: relative;
      bottom: auto;
      left: auto;
      margin-top: 1rem;
      padding: 1rem 1.5rem;
      display: inline-flex;
      flex-direction: column;
      align-items: center;
      gap: 0.3rem;
    }
    .exp-number {
      font-size: 1.6rem;
    }
    .exp-text {
      font-size: 0.85rem;
    }
    
    /* Articles Section */
    .articles-section {      padding: 3rem 0;
    }
    .section-header {
      flex-direction: column;
      gap: 1rem;
      margin-bottom: 2rem;
      padding: 0 1rem;
    }
    .section-title {
      margin-bottom: 0;
    }
    .view-all-link {
      font-size: 1rem;
    }
    .articles-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
      padding: 0 1rem;
    }
    .article-card {
      border-radius: 16px;
    }
    .article-image {
      height: 180px;
    }
    .article-content {
      padding: 1.2rem;
    }
    .article-content h3 {
      font-size: 1.15rem;
      line-height: 1.4;
    }
    .article-content p {
      font-size: 0.9rem;
      line-height: 1.6;
    }
    .article-category {
      font-size: 0.75rem;
      padding: 0.3rem 0.8rem;
    }
    .article-meta {
      font-size: 0.85rem;
    }
    
    /* CTA Section */
    .cta-section {
      padding: 3rem 0;
    }
    .cta-content h2 {
      font-size: 1.8rem;      line-height: 1.3;
      margin-bottom: 0.8rem;
      padding: 0 1rem;
    }
    .cta-content p {
      font-size: 1.05rem;
      margin-bottom: 2rem;
      padding: 0 1rem;
    }
    .cta-section .btn-lg {
      max-width: 90%;
      margin: 0 auto;
    }
  }
  
  /* ✅ Small Mobile (أقل من 480px) */
  @media (max-width: 480px) {
    .hero-title {
      font-size: 1.6rem;
    }
    .hero-subtitle {
      font-size: 0.95rem;
    }
    .stat-number {
      font-size: 1.7rem;
    }
    .stat-label {
      font-size: 0.85rem;
    }
    .section-title {
      font-size: 1.6rem;
    }
    .about-text {
      font-size: 0.95rem;
    }
    .feature-text h4 {
      font-size: 0.95rem;
    }
    .feature-text p {
      font-size: 0.85rem;
    }
    .article-content h3 {
      font-size: 1.1rem;
    }
  }
  
  /* ✅ تحسينات إضافية للتناسق */
  @media (max-width: 768px) {
    /* توحيد المسافات العمودية */
    section {      margin-bottom: 0;
    }
    
    /* تحسين ظهور البطاقات */
    .stat-card,
    .article-card,
    .feature-item {
      will-change: transform;
      backface-visibility: hidden;
    }
    
    /* منع التكبير غير المرغوب فيه */
    .btn:active {
      transform: scale(0.98);
    }
    
    /* تحسين النصوص الطويلة */
    p {
      word-wrap: break-word;
      overflow-wrap: break-word;
    }
  }
</style>
